name: Build Multi-Arch Caddy

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      # Upgraded v4 -> v4 (or v5 if available, ensuring Node 24 support)
      - name: Checkout Code
        uses: actions/checkout@v4

      # Upgraded v5 -> v7
      - name: Docker Metadata
        id: meta
        uses: docker/metadata-action@v7
        with:
          images: ghcr.io/${{ github.repository_owner }}/caddy
          tags: |
            type=semver,pattern={{version}}
            type=raw,value=latest,enable={{is_default_branch}}

      # Upgraded v3 -> v5
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v5
        with:
          platforms: arm64

      # Upgraded v3 -> v5
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v5

      # Upgraded v3 -> v5
      - name: Login to GHCR
        uses: docker/login-action@v5
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Upgraded v6 -> v7
      - name: Build and Push
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=caddy-v1
          cache-to: type=gha,mode=max,scope=caddy-v1
